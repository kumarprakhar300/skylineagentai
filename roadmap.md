# Roadmap

- [x] Pipeline page: leads grouped by score band + requirement completeness, drag-and-drop stage flow (`/pipeline`)
- [x] Lead import page: upload CSV/XLSX, map columns to the same fields as the export, insert leads (`/import`)
